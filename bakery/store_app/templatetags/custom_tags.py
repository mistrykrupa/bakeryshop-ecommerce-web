# from django import template

# register = template.Library()

# @register.filter(name='has_admin_access')
# def has_admin_access(user):
#     return user.is_superuser or user.groups.filter(name='admin').exists()